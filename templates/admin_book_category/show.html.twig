{% extends 'base-backend.html.twig' %}

{% block title %}BookCategory{% endblock %}

{% block body %}
    <h1>BookCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ book_category.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ book_category.text }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ book_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ book_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_book_category_index') }}">back to list</a>

    <a href="{{ path('app_admin_book_category_edit', {'id': book_category.id}) }}">edit</a>

    {{ include('admin_book_category/_delete_form.html.twig') }}
{% endblock %}
